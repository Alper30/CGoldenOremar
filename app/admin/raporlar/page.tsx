import { SectionPlaceholder } from "@/components/admin/section-placeholder";

export const metadata = { title: "Raporlar · Yönetim" };

export default function Page() {
  return (
    <SectionPlaceholder
      title="Raporlar"
      description="Satış, komisyon, satıcı performansı ve finansal raporların dışa aktarımı (CSV/PDF) burada olacak."
    />
  );
}
